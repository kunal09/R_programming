#Functions
#returns the square root of the number
sqrt(81)
mynum <- 64
sqrt(64)

#c returns the vector of those numbers
v <- c(1, 2, 0.1, -5, 4, 3, 2.9)

#mean - returns the mean of vector of numbers
avg <- mean(v)

#signif - short of significant figures
signif(avg,4)
#it also does the rounding off

#to find documentation of any function 
#type ?<func>

#median
median(v)

#sd for standard deviation
sd(v)

#Exercise 1
min(v)
max(v)
