#Arrays
#its just a array of matrices
bigarray <- array(1:200, dim=c(2,5,10))
message("in dimensions, 
        first para  = row no
        second para = col no
        third para  = matrix no")
bigarray[1,1,5]
bigarray[2,5,10]

#Exercise1 is doubt
#Exercise2 can be easily harcoded